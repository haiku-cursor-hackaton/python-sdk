{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "examples/Dockerfile.basics"
  },
  "deploy": {
    "healthcheckPath": "/.well-known/ucp",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}

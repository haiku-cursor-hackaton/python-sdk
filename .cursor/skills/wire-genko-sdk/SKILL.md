---
name: wire-genko-sdk
description: >-
  Integrate the Genko SDK (genko-sdk) into a Python/FastAPI store: install,
  implement MerchantAdapter, mount UCP REST routers, configure Genko platform
  env vars, and verify discovery. Use when wiring UCP, adding agent checkout,
  installing genko-sdk, merchant adapter, UCP_GATEWAY_API_KEY, or onboarding a
  store to the Genko platform.
---

# Wire Genko SDK into a Store

Read and follow **[docs/AGENT_INTEGRATION.md](../../docs/AGENT_INTEGRATION.md)**
end-to-end. That file is the canonical, agent-agnostic integration guide (works
for Cursor, Codex, Claude Code, and any other assistant).

When executing, load the full guide into context and work through its checklist
in order. Do not skip verification (§6).
